@page "/configurator"
@inject IStringLocalizer<SharedResource> L

@* Placeholder — the real configurator is Phase 19. The Phase 13 home promo links here with a
   ?build=intel / ?build=amd query, which the real page will pick up. *@
<PageTitle>@L["Nav.Configurator"] — PCMarket</PageTitle>

<ComingSoon Title="@L["Nav.Configurator"]" Phase="19" />
